# Sample Data

Place tiny, non-sensitive images or metadata fixtures here for tests and local
demos. Do not store full datasets or private exports in this directory.

Only image files with supported extensions are treated as sample inputs by the
local data helper. This README is intentionally ignored by the loader.
