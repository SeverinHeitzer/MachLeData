# Data Source Notes

Document the BigQuery datasets, table schemas, image storage locations, and
label formats used by the object detection workflow.

Do not include secrets or private dataset exports. Use `.env.example` for
required variable names and `data/samples/` for tiny test fixtures only.
