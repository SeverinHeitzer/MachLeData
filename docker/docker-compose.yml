# Local service composition for development.
# Add API, dashboard, and workflow services here as their commands stabilize.

services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command: python -m uvicorn apps.api:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
