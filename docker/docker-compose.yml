# Local service composition for API and dashboard development.

services:
  api:
    image: ${MACHLEDATA_IMAGE:-machledata:local}
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command: python -m uvicorn apps.api:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
