# Model and training configuration for the object detection pipeline.
# Tune these values as the YOLO model and dataset choices become concrete.

model_name: yolov8n
image_size: 640
batch_size: 8
epochs: 10
artifact_dir: artifacts/models

